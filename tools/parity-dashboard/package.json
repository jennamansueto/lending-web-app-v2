{
  "name": "parity-dashboard",
  "version": "1.0.0",
  "description": "Live parity dashboard for the four verification levels. The server itself has no dependencies; playwright is only used by record-run.mjs to record a run.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
