# Parity dashboard

Generated by `bash parity/run-all.sh` at 2026-09-23T08:54:43Z. Comparisons are exact: tolerance is zero at every
level.

**Overall: PASS**

| Level | Status | Passed | Failed | Total | Duration | Report |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| Layer 1 — UI | PASS | 42 | 0 | 42 checks | 33s | `parity/reports/l1-ui.json` |
| Layer 2 — Service | PASS | 780 | 0 | 780 tests | 2s | `parity/reports/l2-service.json` |
| Layer 3 — Data | PASS | 45 | 0 | 45 checksums | 2s | `parity/reports/data-parity.json` |
| Layer 4 — Workflow | PASS | 3 | 0 | 3 scenarios | 6s | `parity/reports/l4-e2e.json` |
| **All levels** | **PASS** | **870** | **0** | **870** | | `parity/reports/parity-dashboard.json` |

## Layer 1 — UI

Angular shell and remotes driven in a browser, every rendered value compared to the golden corpus, the service and the system of record.

```
$ bash parity/run-l1.sh
L1: 42/42 checks passed
```

## Layer 2 — Service

Golden corpus replayed against the domain, grouped by business rule.

```
$ bash parity/run-l2.sh
L2: 780/780 tests passed
```

## Layer 3 — Data

Fresh Oracle to Postgres migration, then row counts, numeric sums and table checksums.

```
$ bash parity/run-l3.sh
L3: 45/45 checksums passed
```

## Layer 4 — Workflow

Temporal origination workflow scenarios run end to end against the live stack.

```
$ bash parity/run-l4.sh
L4: 3/3 scenarios passed
```
